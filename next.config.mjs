/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  images: {
    domains: [
      "lh3.googleusercontent.com",
      "i.pravatar.cc",
      "clip-it.cloud",
      "localhost",
    ],
  },
};

export default nextConfig;
